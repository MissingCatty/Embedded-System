`Bitaction`为一个枚举类型，定义为：

```c
typedef enum
{ Bit_RESET = 0,
  Bit_SET
}BitAction;
```

主要用在函数`void GPIO_WriteBit(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin, BitAction BitVal)`里，在使用时指定引脚的电平为高电平还是低电平，第三个参数`BitAction BitVal`，如果传入的是一个int类型的变量，则有如下的规则：

- BitVal ≠ 0，置引脚为高电平
- BitVal = 0，置引脚为低电平