# 一、按键

## 硬件电路

- 低电平表示按键按下（**常用**）
  - 左图按键未按下时，PA0悬空，此时输入状态未定（不允许），所以应该将PA0配置成**上拉输入模式**
  - 右图按键未按下时，PA0接上拉电阻，此时默认为高电平

![image-20231231084017235](https://zyc-learning-1309954661.cos.ap-nanjing.myqcloud.com/machine-learning-pic/image-20231231084017235.png)

- 高电平表示按键按下

![image-20231231084504243](https://zyc-learning-1309954661.cos.ap-nanjing.myqcloud.com/machine-learning-pic/image-20231231084504243.png)

# 二、实现的功能

- 两个按键分别对应两个LED
- 一个按键按下，其对应的LED切换状态（原来熄灭的亮起来，原来亮的熄灭）

# 三、代码

## Key.c

```c
#include "stm32f10x.h"                  // Device header
#include "Delay.h"

void Key_Init(void)
{
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE);
	GPIO_InitTypeDef GPIO_InitStructure;
	
	//初始化输入引脚
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_9 | GPIO_Pin_11;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOA,&GPIO_InitStructure);
}

uint8_t Key_GetNum(void)
{
	uint8_t KeyNum = 0;
	if(GPIO_ReadInputDataBit(GPIOA,GPIO_Pin_9)==0)
	{
		Delay_ms(20);
		while(GPIO_ReadInputDataBit(GPIOA,GPIO_Pin_9)==0);	//延时消抖：第一次检测出低电平，但不能保证就是按键按下了，还要经过20ms第二次验证
		Delay_ms(20);
		KeyNum = 1;
	}
	if(GPIO_ReadInputDataBit(GPIOA,GPIO_Pin_11)==0)
	{
		Delay_ms(20);
		while(GPIO_ReadInputDataBit(GPIOA,GPIO_Pin_11)==0);
		Delay_ms(20);
		KeyNum = 2;
	}
	return KeyNum;
}

```

## LED.C

```C
#include "stm32f10x.h"                  // Device header

void LED_Init(void)
{
	RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA,ENABLE);
	
	GPIO_InitTypeDef GPIO_InitStructure;
	GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0 | GPIO_Pin_1;
	GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
	GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
	GPIO_Init(GPIOA,&GPIO_InitStructure);
}

void LED_On(int LEDNum)
{
	if(LEDNum == 1)	//A0 LED翻转
	{
		uint8_t in1 = GPIO_ReadOutputDataBit(GPIOA,GPIO_Pin_0);	//读取A0口的输出值
		if(in1)
			GPIO_WriteBit(GPIOA,GPIO_Pin_0,Bit_RESET);
		else
			GPIO_WriteBit(GPIOA,GPIO_Pin_0,Bit_SET);
	}
	else if(LEDNum == 2)	////A1 LED翻转
	{
		uint8_t in2 = GPIO_ReadOutputDataBit(GPIOA,GPIO_Pin_1);	//读取A1口的输出值
		if(in2)
			GPIO_WriteBit(GPIOA,GPIO_Pin_1,Bit_RESET);
		else
			GPIO_WriteBit(GPIOA,GPIO_Pin_1,Bit_SET);
	}
}

```

# main.c

```c
#include "stm32f10x.h"                  // Device header
#include "Delay.h"
#include "LED.h"
#include "Key.h"

int main()
{	
	Key_Init();
	LED_Init();
	uint8_t KeyNum;
	while(1)
	{
		KeyNum = Key_GetNum();
		LED_On(KeyNum);
	}
}

```

